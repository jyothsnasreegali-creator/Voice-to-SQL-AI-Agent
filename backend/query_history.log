User: add a record door for john
SQL: CREATE TABLE IF NOT EXISTS products (id INTEGER PRIMARY KEY, name TEXT, price REAL);

CREATE TABLE IF NOT EXISTS orders (id INTEGER PRIMARY KEY, item_name TEXT, price REAL);

CREATE TABLE IF NOT EXISTS employees (id INTEGER PRIMARY KEY, name TEXT, department TEXT, hire_date DATE, salary REAL);

CREATE TABLE IF NOT EXISTS sales (id INTEGER PRIMARY KEY, employee_id INTEGER, product TEXT, region TEXT, amount REAL, sale_date DATE);

CREATE TABLE IF NOT EXISTS student (id INTEGER PRIMARY KEY, student_diaries TEXT);
---
User: google
SQL: CREATE TABLE IF NOT EXISTS orders (
    id INTEGER PRIMARY KEY,
    item_name TEXT NOT NULL,
    price REAL NOT NULL
);

CREATE TABLE IF NOT EXISTS products (
    id INTEGER PRIMARY KEY,
    name TEXT NOT NULL,
    price REAL NOT NULL
);

CREATE TABLE IF NOT EXISTS sales (
    id INTEGER PRIMARY KEY,
    employee_id INTEGER NOT NULL,
    product INTEGER NOT NULL,
    region TEXT NOT NULL,
    amount REAL NOT NULL,
    sale_date DATE NOT NULL,
    FOREIGN KEY (employee_id) REFERENCES employees (id),
    FOREIGN KEY (product) REFERENCES products (id)
);

CREATE TABLE IF NOT EXISTS student_diaries (
    id INTEGER PRIMARY KEY,
    student_diaries TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS students (
    id INTEGER PRIMARY KEY,
    student_diaries TEXT NOT NULL
);
---
